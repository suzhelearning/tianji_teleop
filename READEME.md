# Tianji 遥操与数据采集

## 启动前准备

打开 **4 个终端**，每个终端都先进入项目目录：

```bash
cd ~/syz/tianji_teleop-ros2
```

以下以操作人员 `kj` 为例。更换人员时，PICO 和 Manus 的 `--user` 应使用同一人员名称。

## 启动顺序

### 终端 1：PICO

```bash
bash bash/run_pico.sh --user kj
```

保持终端打开。需要设置地面时，按右手柄 **A**，查看 `Set Ground` 和地面锁定日志。

> 地面设置应在真机使能前完成。

### 终端 2：Manus

```bash
bash bash/run_manus.sh --user kj
```

保持终端打开。

### 终端 3：相机与头显视频

```bash
bash bash/run_camera_views.sh
```

保持终端打开。

### 终端 4：真机遥操与采集

**前三路就绪后**运行：

```bash
bash bash/run_teleop.sh --data --task pick-and-place
```

### 先做仿真（替代终端 4 的真机命令）

保持 PICO 和 Manus 运行，先在终端 4 启动：

```bash
bash bash/run_teleop.sh --sim
```

- 默认从 `/wuji/left_hand/joint_commands` 和 `/wuji/right_hand/joint_commands` 订阅 Manus 的双手目标；话题由 `config/robot.json` 配置。
- 点击仿真窗口后，按 `S` 开始跟随，按 `P` 或空格保持，按 `H` 让双臂平滑回 Home 后等待。
- 手指仅在 `TELEOP` 状态跟随；等待、保持、回 Home 或故障时保持姿态。单手数据过期或无效时，该手停止更新。
- 仿真不驱动真机、不导出关节命令。切换真机前，先退出仿真。
- 仅看双臂：`bash bash/run_teleop.sh --sim --no-hand-teleop`。
- 使用外骨骼而非 Manus 时，显式指定 `--hand-source exoskeleton`，接收本机 TJH2 UDP（默认端口 `16000`，可用 `--hand-port` 修改）。

> 以下“遥操与采集操作”的终端按键用于真机模式，不是仿真窗口的 `S/H/P`。

## 遥操与采集操作

以下按键均在 **终端 4** 中操作：

| 按键 | 操作与注意事项 |
| --- | --- |
| 首次 `r`（`WAITING`） | 确认机器人在 Home、双手空手且空间安全后，使能并缓慢张手。 |
| 再次 `r`（`HOME_READY`） | 查看 PICO 双臂与 Manus 双手目标虚影，确认空手及路径安全后，冻结当前目标并缓慢靠近；到位停稳、录制确认后自动开始跟随。 |
| `s` | 立即截止并保存数据，停止跟随并限速制动；停稳且保存确认后缓慢回 Home、张手。 |
| `d` | 立即截止并丢弃数据，运动收尾与 `s` 相同。 |

- `r`、`s`、`d` **不要追加回车**。
- 准备状态的青色虚影实时显示操作者目标；按 `r` 后冻结，靠近途中改变姿态不会改变本次对齐目标。
- 不再要求操作者双手放腰间或手指目标接近全零。机器人仍须在 Home 停稳、完成张手后才能开始对齐。
- 靠近与等待录制确认期间不跟随实时动作；靠近阶段不录制。确认录制后通过限速衔接实时目标。
- 若采集明确拒绝启动且保持空闲，机器人停在已对齐姿态，排除问题后重新按 `r` 重试录制；`Enter` 或 `Ctrl+C` 可中止并失能。
- 慢速对齐不保证路径无碰撞；按 `r` 前确认目标及整个运动空间安全。
- 按 `s` 或 `d` 后立即接管，不再需要人靠近 Home；机械臂先限速制动，双手保持，再等待实测停稳和数据操作确认后回位、张手。请事先确认回 Home 路径安全；`s/d` 不是物理急停。

## 停止顺序

1. 在 **终端 4** 按 `Ctrl+C`。
2. 确认机器人停止、执行器退出。
3. 再停止终端 1–3。
