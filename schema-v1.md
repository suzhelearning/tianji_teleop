# 数据采集 HDF5 Schema

## 1. 范围

面向 Tianji 双臂、Wuji Hand 2 双手和 RealSense RGB。

一个 episode 对应一个 HDF5 文件，**只保存真实关节位置、RGB、各自时间戳和必要元数据**。
不保存参考动作、插值后的下发命令、深度、IMU、速度、电流或训练数据副本。

训练端由使用者通过时间偏移构造 state/action 配对；采集器不生成动作标签，
也不把未来实测位置声明为实际下发的参考动作。因此文件没有 `actions` 组或 `action_type` 属性。

## 2. 频率与维度

| 数据 | 原始采集频率 | 内容 |
|---|---:|---|
| 双臂实际关节位置 | 120 Hz 目标 | 左 7 + 右 7，共 14 维 |
| 双手实际关节位置 | 120 Hz 目标 | 左 20 + 右 20，共 40 维 |
| RGB | 每路 30 fps 目标 | 1280 × 720，源格式 RGB8，采集存储为未压缩 RGB |
| 建议训练时间网格 | 30 Hz | 训练端自行按时间戳对齐 |

所有关节角使用 rad。120 Hz 是实际状态读取和记录目标，不修改设备内部伺服频率。
每条样本使用真实时间戳，不假设间隔严格等于 1/120 秒；实际频率必须由时间戳统计。

## 3. 文件结构

```text
/data/TianjiData/raw/
└── 20260913/
    ├── dataset_config.json
    ├── 20260913_143025_123456_take001.h5
    └── 20260913_143102_654321_take002.h5
```

```text
20260913_143025_123456_take001.h5
│
├── @schema_version = 1
├── @task                         # 任务标识，UTF-8
├── @success                      # bool，操作者确认的任务结果
├── @robot_config                 # 如 tianji_wuji2_v1
│
├── observations/
│   ├── arms/
│   │   ├── timestamp_ns          int64 [Na]
│   │   └── qpos                  float32 [Na,14]
│   └── hands/
│       ├── timestamp_ns          int64 [Nh]
│       └── qpos                  float32 [Nh,40]
│
└── images/
    ├── top/
    │   ├── timestamp_ns          int64 [Ft]
    │   └── rgb                   uint8 [Ft,720,1280,3]
    ├── left_wrist/
    │   ├── timestamp_ns          int64 [Fl]
    │   └── rgb                   uint8 [Fl,720,1280,3]
    └── right_wrist/
        ├── timestamp_ns          int64 [Fr]
        └── rgb                   uint8 [Fr,720,1280,3]
```

`Na`、`Nh` 和各相机帧数独立，不要求相等。同一组内时间戳数量必须与数据行数一致。
只创建实际使用的相机组，不创建空流；相机配置值为整数 `0` 时表示禁用，不生成全零图片。
当前默认启用 top、left_wrist、right_wrist 三路，写入 `/data/TianjiData/raw/YYYYMMDD/`。
原两相机及三相机 JPEG 数据集仍按各自的 `dataset_config.json` 读取，不覆盖旧契约或补造缺失相机流。

原始 `rgb[i]` 是一帧 RGB 通道顺序的 uint8 像素，HDF5 chunks 为 `(1,720,1280,3)`，不启用压缩或其他过滤器。
离线压缩后在独立日期数据集 `/data/TianjiData/compressed/YYYYMMDD_compressed/` 中，将每路 `rgb` 替换为 `jpeg: vlen uint8 [F]`；
每个 `jpeg[i]` 是一帧完整 JPEG 的编码字节。一个相机组只包含 `timestamp_ns` 和对应编码的数据集，不同时保存两份图像。
旧 JPEG Q90 数据继续兼容；固定 Q50 的批量脚本同时接受原始 RGB 和已有 JPEG。JPEG 输入经解码后二次有损编码为 Q50，仅写入独立输出目录，不修改源数据或其配置。

## 4. 真实关节状态

拼接后的状态顺序固定为：

| 列范围，左闭右开 | 内容 |
|---|---|
| `[0:7]` | 左臂 |
| `[7:14]` | 右臂 |
| `[14:34]` | 左手 |
| `[34:54]` | 右手 |

数据集配置给出按此顺序排列的 54 个真实关节名称。
关节零位、正方向或机器人配置改变时，使用新的 `robot_config`，不得无标记混合。

`observations/*/qpos` 必须来自实际反馈，不使用目标角度代替。
采集器分别形成双臂 14 维快照和双手 40 维快照；这是采集器快照，
不声称左右设备在硬件上同步采样。检查 SDK 有效性及新反馈标记，
不把已知失效反馈或重复缓存当作新反馈。双手只有左右各自均有新反馈才形成下一条 40 维记录。

## 5. 时间戳

- 使用同一采集主机的单调时钟，以本 episode 录制起点为零，单位 ns，存为 int64。
- 状态时间戳：完整状态快照在公共输入缓冲区可用的时间。
- 图像时间戳：完整 RGB 帧在公共输入缓冲区可用的时间；离线压缩逐项原样保留，不重采样或补帧。
- 录制开始前的缓存不补写为零时刻，也不重打时间戳冒充新样本。
- 各流时间戳单调非递减；同一时间戳有多个样本时，按存储顺序取最后一条。
- `--data` 的 `s/d` 截止点是执行器读取单次按键时的主机单调时钟，不是保存 RPC 完成时间。
  保存时保留时间戳小于等于截止点的样本，并裁掉队列、批缓冲及已写入流中晚于截止点的行。
  正截止点的文件额外保存根属性 `start_monotonic_ns`、`end_monotonic_ns`；
  各流的相对时间戳不得大于两者之差。旧的无显式截止点文件仍可读取。

本版不保存设备源时间戳、曝光时间或多机时钟映射。它支持按在线可用性对齐，
不用于精确分析传感器、传输和执行延迟。多主机各自单调时钟不可直接混用。

## 6. 数据集级配置

每个日期目录内的 `dataset_config.json` 保存该日共享配置，与 `.h5` 文件同级，不在每个 episode 重复完整内容。`raw/` 和 `compressed/` 汇总根目录不写共享配置；压缩输出的每个日期目录包含其独立配置。

| 字段 | 要求 |
|---|---|
| `schema_version` | `1` |
| `robot_config` | 与 episode 属性一致 |
| `joint_names` | 按固定顺序排列的 54 个真实关节名 |
| `joint_unit` | `rad` |
| `policy_rate_hz` | `30`，训练端可选的名义网格，不是采样间隔保证 |
| `camera_names` | 实际使用的有序相机名称列表 |
| `image_width` / `image_height` | `1280` / `720` |
| `image_encoding` | 采集为 `rgb`；离线压缩或旧 JPEG 数据为 `jpeg` |
| `decoded_color_order` | `RGB` |
| `jpeg_quality` | `rgb` 必须为 JSON `null`；批量压缩输出为 `50`，旧 JPEG 数据保留其原质量值 |

同一日期数据集保持上述契约一致。配置不匹配时拒绝追加，不覆盖旧配置；不同日期可使用不同配置，采集按预留 episode 路径的日期选择配置目录。
运行用的相机序列号与禁用槽位放在 `collection_config.json`；数据集配置不保存禁用相机。
相机内外参不是本版训练字段，不纳入此 schema。

### 离线批量压缩

`bash compress_data.sh [原始目录 [输出目录]]` 默认从 `/data/TianjiData/raw/YYYYMMDD/` 生成 `/data/TianjiData/compressed/YYYYMMDD_compressed/`，按日期独立读取和校验配置，JPEG 质量固定为 50。`bash compress.sh --date YYYYMMDD` 只处理指定日期；单个含配置的独立数据集也可显式传入。
只处理已完成 `.h5`（包括明确标记 `success=false` 的完成文件），跳过 `.partial.h5` 和符号链接；
不删除或覆盖原始数据。默认日期目录添加 `_compressed` 后缀，显式指定输出目录时保持相对路径；保持关节值、时间戳、任务与成功标记和其他属性，输出另加源文件 SHA256、源配置 SHA256 和日期数据集内相对路径溯源属性。
输出先写独有临时文件，校验并同步后以不覆盖的原子方式发布；中断后重复运行可继续，已完成输出须通过源身份与格式校验才跳过。
源/目标目录不得重叠，目标配置冲突、活动文件锁或坏文件会明确失败；独立文件可继续处理，最终返回非零状态并汇总失败数。
原始图像约 249 MB/s、14.9 GB/分钟，压缩输出另占空间；原始保留策略意味着压缩本身不会释放采集盘空间。

## 7. 操作员录制生命周期

采集器没有运动权限；下面的操作流程由 `bash bash/run_teleop.sh --data --task LABEL`
协调。四终端入口保持不变：

```bash
bash bash/run_pico.sh --user kj
bash bash/run_manus.sh --user kj
bash bash/run_camera_views.sh
bash bash/run_teleop.sh --data --task pick-and-place
```

终端 1 的 `run_pico.sh` 保持前台运行，不创建 tmux；实时显示带 `[driver]`、`[m0]`、
`[bridge]` 前缀的日志，并保留各进程日志文件。右手柄 A 的 `Set Ground` 事件和地面锁定结果
会出现在该终端。输入就绪仍要求新鲜匹配的手臂数据及本次骨架窗口的实际渲染心跳。
退出顺序是先在终端 4 停止真机执行器，再在终端 1 按 `Ctrl+C`；只清理本次启动拥有的输入进程。
旧 tmux 输入不会被自动接管或停止：确认执行器退出后，显式运行 `bash bash/run_stop_pico.sh`
停止本工程旧后台会话，再启动前台命令。标定向导/仿真的内部后台模式保留；
仿真可以复用人员和标定一致的前台输入，但退出仿真不会关闭该输入终端。

首次 `r` 前确认机器人处于 Home、双手无物体且张手空间安全；使能后机械臂保持实测位置，
双手受限张开。`r/s/d` 都是单次按键，不加回车，也表达操作者对空手条件的确认。

| 按键 | 数据与运动行为 |
|---|---|
| `r` | `WAITING` 时使能并缓慢张手；Home 准备完成后再次按下，冻结预览中的 PICO 双臂与 Manus 双手目标，缓慢对齐；到位停稳、录制确认后才开始跟随 |
| `s` | 以按键时刻截止本条数据并后台保存，`success=true`；立即停止跟随并限速制动，停稳且保存确认后自动回位 |
| `d` | 以按键时刻停止并丢弃本条数据；运动收尾与 `s` 相同，不删除以前保存的 episode |

使用 PICO 当前绝对映射的双臂目标及 Manus 双手目标，不建立每条采集的腰间相对参考。
准备状态显示实时青色目标虚影；按 `r` 后冻结全部关节目标，靠近期间操作者动作不会改变对齐目标。
对齐阶段不录制；到位停稳后保持冻结姿态，收到成功启动响应和匹配的录制状态后，才限速衔接实时目标。
若采集明确拒绝启动且确认空闲，保持已对齐姿态，必须重新踩 `r` 重试录制；结果不明或故障时停止，不自动重试。
`s/d` **不是急停**：按键后立即接管，机械臂沿当前下发速度限速制动，双手保持最后目标，
不再跟随操作者动作，也不要求先进入 Home 邻域。实测停稳并确认数据操作完成后，
再缓慢回 Home，最后张手；全部完成才允许下一条。制动、回位和张手不进入已截止的数据。
`q` 在 Home 结束本场；录制中 `q` 先截止保存，再按同一制动/回位流程结束。

使能后 `Enter`、`Ctrl+C`、关闭监视窗口或跟踪/反馈故障均停止，不自动回 Home，
不自动重新接管；未正常结束的数据保留 `.partial.h5`，不拼接中断两侧。
采集器本身只报告数据故障，是否停止设备由执行器安全边界负责。
纯遥操 `--real` 的原有回车对齐/遥操/Home 流程保持不变。

头显文字仅叠加在发送给 PICO 的 top camera 视频副本上，左右眼一致；采集原图不含文字，
不播放提示音。需要 Pillow 与系统 `fonts-noto-cjk`。控制状态过期会替换成失联警告；
视频发送成功不等于头显已实际显示。

`config/robot.json` 的 `staged_motion.episode` 配置机器人张手目标及实测张手容差。
不再检查操作者腰间区域、掌心稳定窗口或手部零姿态；机器人实测 Home、张手和静止条件仍保留。
慢速对齐/回位默认速度 0.2 rad/s、加速度 0.2 rad/s²，不再配置或检查 Home 接管距离。
实测静止速度阈值 0.03 rad/s、稳定时间 0.2 s。靠近与等待确认都不能绕过命令断流和硬件保护。
这些是可调启动值，未替代实际工作区、持物条件和真机低速验证，尤其不证明回位路径无碰撞。

不再生成每条相对参考的 `episode_references` 日志；现有关节目标、实际发送命令及反馈仍由运行日志记录。
此流程不改变 schema-v1 的数据字段，也不把靠近阶段作为动作标签。日志在设备退出后落盘。
每段停止时在同一发布锁内检查采集进度并停止接收，冻结段内故障；
后台保存只检查该段冻结的采集状态和写入校验结果，不使用截止后的采集服务故障否决旧段。

## 8. 写入与最小校验

- 使用单个后台 HDF5 写入者；JPEG 编码、写盘和保存校验不阻塞控制循环。
- 数据集首维可追加；队列有界，溢出必须明确报告并结束当前数据段，不静默丢帧。
- 正在录制的文件以 `.partial.h5` 结尾，收到 `s` 后关闭并校验，通过后发布为 `.h5`。
- 验证字段长度、关节维数、有限数值、时间戳顺序、配置匹配及 JPEG 可解码性。
- 校验失败的文件保持 `.partial.h5`，不会覆盖已有完整 episode。
- 未按 `s` 的退出不是成功确认；按 `d` 是显式丢弃，不保留该段训练文件。
- 录制中的进度检查不依赖采样线程返回；状态反馈/发布超过 300 ms、图像发布超过 2 s 未更新即报告故障。
- 写入者初始化被取消后，在文件操作返回时清理未开始录制的文件，不留下等待数据的孤立线程。
- 关闭超时必须报告失败并保留写入者管理权；确认关闭前不得报告完成或开始新段，退出时重试清理。

## 9. 训练端职责与排除项

训练端自行选择时间偏移、时间网格、窗口长度、state/action 配对以及图像预处理。
不得把本数据集中并未记录的控制命令当作已知标签。不要跨 episode 或已知断流区间构造样本。

不在采集文件中重复保存 30 Hz 副本、动作块、缩放图像、归一化数据，
也不保存参考动作、插值后命令、深度、IMU、速度、电流、力矩或设备源时间戳。

核心数据保持为：**真实关节状态 + RGB + 各自时间戳**。
