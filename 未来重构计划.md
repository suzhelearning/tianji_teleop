---
title: 未来重构计划
status: draft
type: note
updated: 2026-09-17
---

# 未来重构计划

本文整理本次关于 Tianji Arm + Wuji2 Hand 遥操作仓库的需求与目录组织讨论。**这是未来架构建议，不是已实施的重构，也不代表所有接口已最终确定。** 本文仅保存在当前仓库，不同步到个人知识库。

## 1. 项目定位与目标范围

本项目服务于 Tianji 双臂与 Wuji2 双手，覆盖人类遥操作、真机与仿真执行、HDF5 数据采集，以及外部模型输出的执行接入。

### 1.1 真机遥操作

- 手部输入有两种：Manus 磁定位手套、自研手部外骨骼。
- 手套只控制 Wuji2 Hand，不控制机械臂。
- 目标设计中，两种手套都输出 MANO 手型，通过同一套 retargeting 转换为 Wuji2 关节目标。
- Tianji Arm 目前通过 PICO VR 控制末端位姿。
- 未来可能接入上肢外骨骼设备，需要在架构上预留接入位置，目前尚未使用。

### 1.2 仿真遥操作

- 使用 PICO VR 或 Quest VR。
- 手势识别模式同时提供腕部位姿和手部手型。
- 腕部位姿对应 Tianji 机械臂末端目标。
- 手势提供类似 MANO 的手型，复用同一套 Wuji2 retargeting。
- 真机和仿真应复用动作转换逻辑，而不是各自维护一套遥操实现。

### 1.3 数据采集

- 真机和仿真都需要采集数据。
- 统一使用 HDF5 保存，以 episode 管理录制生命周期。
- 需要统一数据语义，同时保留真机、仿真以及仿真执行模式的区别。

### 1.4 外部 policy 接入

仓库只接收模型输出并执行，**不包含模型加载或模型推理**。预留以下输出的接入能力：

- 机械臂末端轨迹，经 IK 后执行。
- 手部 RL policy 的动作输出。
- 双臂与双手统一 VLA policy 的动作输出。

具体动作语义尚需定义，不能仅凭 RL 或 VLA 名称判断输出是关节角、增量还是末端位姿。

## 2. 核心组织原则

推荐主链路：

```text
输入来源 → 统一动作表示 → 动作转换 → 安全执行 → 真机／仿真
```

数据记录是贯穿链路的独立能力，而不是某个执行模式的附属实现。

不建议把 `real_teleop/`、`sim_teleop/`、`policy/` 建成三套完整系统。它们主要差异是输入来源和执行后端，手部重定向、机械臂 IK、适用的安全约束和数据格式应复用。

将功能拆成四个可组合维度：

| 维度 | 可选项 |
|---|---|
| 动作来源 | Manus、手部外骨骼、PICO、Quest、未来上肢外骨骼、外部 policy |
| 动作表示 | 手型、机械臂末端位姿／轨迹、关节目标 |
| 执行环境 | Tianji + Wuji2 真机、MuJoCo 仿真 |
| 数据记录 | 不录制、HDF5 episode 录制 |

“真机手套遥操”“VR 仿真遥操”“policy 执行”应优先表示为启动配置，而不是独立业务代码树。

两个重要边界：

1. 输入设备适配不负责机器人 SDK 执行。
2. 执行后端不依赖命令来自手套、VR 还是 policy。

## 3. 目标数据流

```text
Manus ───────────────┐
手部外骨骼 ───────────┤
PICO／Quest 手势 ─────┴→ 统一手型 → Wuji2 retargeting ─┐
                                                     │
PICO／Quest 腕位姿 ─────→ 坐标映射 → Tianji IK ─────────┤
上肢外骨骼（未来）──────→ 统一机械臂目标 ────────────────┤
                                                     ▼
外部 policy → 输出适配 → 按输出类型转换 ────────→ 统一关节命令
                                                     │
                                            安全约束与执行状态机
                                                     │
                                           ┌─────────┴────────┐
                                           ▼                  ▼
                                         真机               仿真

记录层：按已定义的数据契约采集输入、命令、反馈、图像 → HDF5
```

上图中输入、命令等记录项属于未来候选范围，不表示当前 schema 已包含这些数据。

## 4. 建议目录结构

主 Python 包负责编排，项目自有 C++ 计算集中管理；配置、机器人资源、外部依赖与运行产物分离。

```text
tianji_teleop/
├── pyproject.toml
├── CMakeLists.txt
├── README.md
├── src/
│   └── tianji/
│       ├── interfaces/             # 跨模块数据契约
│       │   ├── hand.py             # 统一手型
│       │   ├── arm.py              # 末端目标与轨迹
│       │   ├── command.py          # 机器人关节命令
│       │   └── observation.py      # 机器人状态与图像元数据
│       ├── inputs/                 # 人类操作输入适配
│       │   ├── manus/
│       │   ├── hand_exoskeleton/
│       │   ├── pico/
│       │   └── quest/
│       ├── motion/                 # 独立于输入设备和执行环境
│       │   ├── hand_retargeting/    # 手型 → Wuji2 joints
│       │   ├── arm_mapping/         # 人体／VR 坐标 → 机器人目标
│       │   ├── arm_ik/              # 末端目标／轨迹 → Tianji joints
│       │   └── trajectory/          # 插值、轨迹时间化
│       ├── execution/              # 共用执行流程与保护
│       │   ├── session.py          # 生命周期与状态切换
│       │   ├── arbitration.py      # 输入来源与控制权
│       │   └── safety.py           # 共用约束和有效性检查
│       ├── backends/
│       │   ├── real/
│       │   │   ├── tianji_arm/
│       │   │   └── wuji2_hand/
│       │   └── mujoco/
│       ├── policy_io/              # 外部输出接入，不运行模型
│       │   ├── receiver.py
│       │   └── adapters.py
│       ├── recording/              # 真机／仿真共用 HDF5 采集
│       │   ├── schema.py
│       │   ├── recorder.py
│       │   └── episode.py
│       ├── sensors/                # 相机等观测来源
│       ├── calibration/            # 标定流程与档案管理
│       ├── tools/                  # 离线压缩、回放、可视化
│       └── cli/                    # 启动、检查、标定、录制入口
├── native/                         # 项目自有 C++ 与绑定
│   ├── arm_control/
│   ├── hand_retargeting/
│   └── hand_exoskeleton/
├── configs/
│   ├── robots/                     # 关节约定、限位、Home
│   ├── devices/                    # SN、地址、设备绑定
│   ├── inputs/                     # 输入设备参数
│   ├── motion/                     # IK 与 retargeting 参数
│   ├── sessions/                   # 日常运行的组合配置
│   └── recording/                  # 数据流、相机与记录选项
├── profiles/                       # 按人员管理的标定结果
├── assets/
│   ├── robots/                     # URDF、MJCF、mesh
│   └── hands/                      # 手型资源，按许可管理
├── integrations/                   # 独立环境中的 SDK／ROS 桥接程序
├── third_party/                    # 外部源码、SDK、许可证
├── scripts/                        # 安装、构建与启动脚本
├── tests/
├── docs/
└── runtime/                        # 本地产物，不入版本库
    ├── logs/
    └── datasets/
```

这是职责划分草案，不要求现在创建每个目录和文件。未实现的上肢外骨骼等能力先在设计中预留名称与契约，实际接入时再落代码，避免空模块、占位实现和不必要抽象。

### 独立环境边界

当前主项目与外骨骼分别使用 Python 3.11 和 Python 3.12，还存在 ROS 与厂商 SDK 的运行约束。目录统一不能强迫所有组件进入同一个进程或环境。

- `inputs/`：主系统使用的输入适配。
- `integrations/`：需要独立环境运行的设备采集与桥接程序。
- 二者通过明确协议交接。

**统一数据接口，不等于统一运行环境。**

## 5. 优先明确的接口

### 5.1 手型接口

目标是所有手部输入复用一套 retargeting。但以下表示不能未经转换就视为相同：

- MANO 姿态与形状参数。
- 从 MANO 计算的关节点。
- VR SDK 提供的类似 MANO 的手部骨架。

建议定义明确的 `HandState`。优先考虑固定语义的手部关键点／局部姿态；是否必须包含 MANO 参数，应由重定向真正需要的数据决定。若关键点已足够，不应仅为统一命名让每种设备额外拟合 MANO。

接口至少明确：

- 左右手标识。
- 点或关节顺序。
- 坐标系、轴方向、单位。
- 腕部局部坐标还是世界坐标。
- 采集时间、有效状态。
- 缺失、遮挡、追踪丢失的表达。

腕部全局位姿用于机械臂，手指相对腕部的形状用于灵巧手。两者可分别消费，但要保留时间关联。

### 5.2 机械臂目标接口

分离设备坐标映射与机器人 IK：

```text
设备／人体目标
→ arm_mapping
→ 机器人基座坐标系中的末端目标
→ arm_ik
→ 关节目标
```

PICO 的骨长修正和偏移标定不应混入通用 IK，否则 Quest 和 policy 接入时容易复制求解逻辑。

当前默认 SPARK 还使用肩、肘、腕等人体信息，不能为统一末端位姿接口而丢掉必要输入。可考虑给机械臂目标附带可选人体姿态约束，由各后端明确声明所需字段。

### 5.3 Policy 输出接口

按动作语义选择转换路径，而不是按模型名称复制执行器：

| 输出类型 | 执行路径 |
|---|---|
| 末端位姿／轨迹 | 轨迹处理 → arm IK → 执行 |
| 手部关节目标 | 关节命令 → 执行 |
| 手型目标 | 共用 retargeting → 执行 |
| 双臂与双手联合目标 | 按动作契约转换 → 协调执行 |

接口需要明确绝对／增量、单位、关节顺序、参考坐标系、执行时刻和过期行为。

`policy_io/` 比 `inference/` 更准确：此仓库只接收输出，不承担模型推理。

## 6. 真机与仿真的复用边界

### 应共用

- 动作表示、手部 retargeting、机械臂 IK。
- 命令有效性检查和适用的运动约束。
- episode 生命周期与数据 schema。
- 输入来源选择、启动配置、控制权规则。

### 应分别实现

- 真机连接、设备身份、使能、SDK 调用。
- 仿真步进、状态重置、渲染。
- 真机反馈读取与仿真状态提取。
- 硬件特有保护。

仿真的 `direct` 与 `dynamics` 不能混淆：前者直接显示期望关节状态，后者经过动力学执行。仿真视觉正常不能证明真机跟踪性能或安全性。

## 7. 数据采集契约的未来讨论

当前仓库记录实测双臂关节、双手关节与 RGB，**不保存 actions 或控制目标**。未来是否扩展记录范围，需要单独决定，不能在目录重构中隐式改变现有 schema。

候选数据分类：

| 类别 | 含义 |
|---|---|
| `observations` | 实际反馈、仿真状态、图像 |
| `commands` | 最终提交执行的命令 |
| `source_actions`，可选 | 原始遥操目标或外部 policy 输出 |
| `metadata` | 环境类型、机器人配置、标定版本、控制来源、schema 版本 |

原始 policy 输出、IK 结果、安全约束后的命令不应全部混称 `action`。它们可能不同，这种差异影响回放与训练。

真机与仿真共用 schema 时，还需明确：

- 真机／仿真来源。
- 仿真 direct／dynamics 模式。
- 主机时间与仿真时间的关系。
- 不同频率数据流的时间对齐方式。

当前采集使用 HDF5 episode，关节状态目标读取频率为 120 Hz，三路 RGB 配置为 1280×720、30 FPS；实际频率以时间戳为准。三路原始 RGB 的名义数据量约 14.9 GB/分钟，存储吞吐与容量需要保留为系统约束。

## 8. 当前模块与目标职责的对应

| 当前模块 | 建议最终归属 |
|---|---|
| `tracking/` | PICO 输入、标定、机械臂映射、独立 ROS 桥接 |
| `control/` | 动作求解、原生控制实现、机器人模型、Viewer |
| `manus/`、`exoskeleton/` | 输入适配、独立采集程序、设备配置、原生计算 |
| `retargeting/` | 共用 `motion/hand_retargeting` 与对应 C++ 实现 |
| `real_robot/` | 拆分共用执行逻辑与真机 backend |
| `sim/` | MuJoCo backend，不重复实现动作转换 |
| `data_collection/` | recording、传感器、离线工具 |
| `tianji/` | 扩展为主包与统一命令入口 |

目录迁移不应改变设备身份检查、输入新鲜度检查、人工授权、回 Home 和停止行为。只读预检、离线验证和仿真成功都不能替代实机运动验收。

## 9. 尚未最终确定的事项

1. 统一手型使用 MANO 参数、关键点、局部姿态，还是明确组合。
2. 机械臂目标如何携带 SPARK 所需的人体姿态信息。
3. 外部 policy 的具体动作语义、传输方式和时间契约。
4. 是否扩展现有 HDF5 schema，保存命令与原始动作。
5. 哪些设备采集进程必须保留独立环境。
6. 最终目录命名和实际迁移顺序。

## 10. 下一步建议

先确定四个核心契约：**手型、机械臂目标、执行命令、记录数据**，再确定最终目录和迁移计划。

用两条代表性链路检验职责边界：

- PICO + 手套 → 真机。
- VR 腕部 + 手势 → 仿真。

如果两条链路能够共用动作转换与记录层，未来上肢外骨骼和 policy 输出接入就不需要再重构主体。本次只归档讨论，不搬移代码、不创建新架构模块、不改变运行行为。

## 相关仓库资料

- [项目说明](README.md)
- [现有数据契约](schema-v1.md)
- [双臂控制说明](control/README.md)
- [mapped-palm 移植说明](docs/mapped-palm-port.md)
- [mapped-palm 真机边界](docs/mapped-palm-real-readiness.md)
