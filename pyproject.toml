# Packages are installed by colcon; the workspace root is not a Python package.
[project]
name = "tianji-spd"
version = "0.3.0"
description = "PICO bare-hand DLS and Hand2 control client for SPD simulation"
requires-python = ">=3.12"

[tool.setuptools]
packages = []

[tool.pytest.ini_options]
testpaths = ["src/teleop_inputs/pico_hand/python/pico2_hands/tests"]
