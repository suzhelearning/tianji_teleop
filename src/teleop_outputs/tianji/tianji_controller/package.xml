<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>tianji_controller</name>
  <version>0.3.0</version>
  <description>
    Native controller profile resources plus the TJRC command-frame codec
    (``protocol``) that decodes the loopback frame emitted by the native DLS
    worker. Also hosts the native CMake project (core QP/IK, Ruckig and the
    PICO shared-root DLS worker) under ``native/``. It declares no ROS
    interfaces and grants no motion authority.
  </description>
  <maintainer email="ops@example.com">Tianji operators</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>ament_cmake_python</buildtool_depend>

  <exec_depend>tianji_description</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
